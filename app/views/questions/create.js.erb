$('.question-errors').html('<%= render 'shared/errors', resource: @question %>')
$('.notice').html('<%= notice %>')
$('.alert').html('<%= alert %>')
<% if @question.persisted? %>
  $('#question_body').val('')
  $('#question_title').val('')
<% end %>